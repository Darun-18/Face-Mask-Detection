# Face-Mask-Detection
A real-time face mask detection system built using YOLO11 and OpenCV that detects whether a person is wearing a face mask through a webcam and automatically sends an email alert with the captured image whenever a "No Mask" person is detected.
